Posso evitare heartbeat?

    Se mi presento a un robot che non c'è?
    lo tolgo da otherRobots     + avviso il server (+ avviso gli altri)

    Se dico che me ne sto andando a un robot che non c'è?
    (lo tolgo da otherRobots)   + avviso il server  (+ avviso gli altri)


PM10Simulator           acts like a PRODUCER, puts Measurement(s) in BufferImpl
SensorDataProcessing    acts like a CONSUMER, gets windows of 8 Measurement(s) from BufferImpl
SensorDataProcessing    acts like a PRODUCER, puts avg of windows in BufferAverages
SensorDataPublishing    acts like a CONSUMER, gets avg windows from BufferAverages and publish them via MQTT


/* considerazioni in maintenance thread
 * Se si aggiunge un nuovo robot e io ho già mandato le richieste senza considerare lui
 * perchè non lo avevo in otherRobots non è un problema :
 * se non lo avevo in otherRobots non può aver ancora mandato richieste con un timestamp < del mio.
 *
 * Se un robot termina in modo controllato (con "quit") non succede nulla
 * perchè prima di terminare manda tutte le risposte che ha in sospeso.
 */

 - sopprimi grpc
 - formatta codice
